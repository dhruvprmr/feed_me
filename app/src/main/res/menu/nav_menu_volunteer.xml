<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">

        <item
            android:id="@+id/menu_home_volunteer"
            android:icon="@drawable/home"
            app:showAsAction="never"
            android:title="Home"/>

        <item
            android:id="@+id/donation_req_volunteer"
            android:icon="@drawable/donate"
            android:title="Donations Requests"
            app:showAsAction="never"/>

        <item
            android:id="@+id/menu_settings_volunteer"
            android:icon="@drawable/settings_img"
            app:showAsAction="never"
            android:title="Settings" />
        <item
            android:id="@+id/menu_logout_volunteer"
            android:icon="@drawable/logout"
            app:showAsAction="never"
            android:title="Log Out" />

    </group>
</menu>